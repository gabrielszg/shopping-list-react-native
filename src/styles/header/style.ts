import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
  },

  title: {
    fontSize: 25,
    fontWeight: 'bold',
    marginVertical: 8,
    textAlign: 'center',
    color: '#000',
  },

  logo: {
    width: 75,
    height: 75,
    alignSelf: 'center',
  },
});
